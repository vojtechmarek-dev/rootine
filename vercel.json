{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then npm run db:migrate; fi && npm run build",
    "crons": [
        {
            "path": "/api/cron/reminders",
            "schedule": "*/15 * * * *"
        }
    ]
}
